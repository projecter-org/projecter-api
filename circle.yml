machine:
  python:
    version: 2.7.10
  ruby:
    version: 2.4.1
  rails:
    version: 5.1.2

  dependencies:
    pre:
      - pip install ansible
      - pip install --upgrade setuptools
  
  test:
    override:
      - echo "test"

  general:
    artifacts:
      - "dist/"

  deployment:
    prod:
      branch: master
      commands:
        - ansible-playbook deployment/deploy.yml -i deployment/vars
